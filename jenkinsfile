node {
  def app
  stage('Clone repository'){
    checkut scm
  }
  stage('Compile') {          	 
    steps {               	 
      sh "mvn compile"          	 
    }     	 
  }     	 
  stage("Unit test") {          	 
    steps {               	 
      sh "mvn test"          	 
    }     	 
  }
}
