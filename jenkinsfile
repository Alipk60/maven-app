pipeline {
  stage('Clone repository'){
    checkout scm
  }
  stage('Compile') {          	 
    steps {               	 
      sh "mvn compile"          	 
    }     	 
  }     	 
  stage("Unit test") {          	 
    steps {               	 
      sh "mvn test"          	 
    }     	 
  }
}
